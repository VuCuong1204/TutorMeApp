package vn.tutorme.mobile.presenter.dialog.bottomsheetpicker

enum class TYPE_PICKER(val value: Int) {
    DEFAULT(0),
    GENDER(1),
    RESOLUTION(2),
    TRANSACTION(3),
    REPORT(4),
    EDIT_OBJECTS(5),
    FILTER_TIME(6),
    SORT(7)
}
